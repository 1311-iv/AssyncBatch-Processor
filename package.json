{
  "name": "asyncbatch-processor",
  "version": "1.0.0",
  "description": "AsyncBatch Processor - Sistema de procesamiento asíncrono de archivos",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
